# 写作 Agent 指导

这个仓库在写一本书。规则按「什么时候需要到达」分层 —— 这本书本身就在讲这件事，
所以这份文件必须以身作则：**只放必须无条件生效的东西，其余进 `research/` 或章节注释。**

## 无条件生效

- **一章一文件，一次改一章。** 不要为了「顺手」跨章编辑 —— 跨章的一致性由
  `make check` 保证，不由你的记忆保证。
- **每章 front matter 必须有 `claim`**，写成「读者能做到 X」，且必须可证伪。
  写完这一章之后自问：把这章删掉，读者还能不能做到 X？能的话这章就是凑页数的。
  禁止用「介绍…」「讲解…」「了解…」开头 —— 那不是承诺，是目录。
- **改完必须跑 `make check`，不通过不算写完。** 退出码 1 是改稿，
  退出码 2 是环境坏了（改稿没有意义）。两者不要混。
- **术语以 `terms.yml` 为准。** 想引入新术语先改 `terms.yml`，单独一个 commit。
- **字数预算以 `budget.yml` 为准。** 改预算是一个决定，单独一个 commit，
  不许和写作混在一起。

## 素材纪律

- **不得照搬源仓库的事故记录。** 事故是论据，不是内容单位。
  每个实例压到 300–800 字，且必须保留事故记录里通常没有的那一半：
  当时相信的是什么 · 为什么那个假设合理 · 什么信号推翻了它。
- **`chapters/` 里不得出现与源仓库排障目录一一对应的文件。**
- **脱敏是重写，不是替换。** 把主机名换成「一台自建 runner」不够，
  要连带说明自建带来什么约束，否则读者不知道这个细节重不重要。
  完整清单见 `research/shapes.md` 末尾。
- **收入管线、选品逻辑、投放策略整体不进书。**

## 行文（参照《重构》的风格）

- **段落要成型，一段讲透一个意思。** 目标 3–6 句。
  单句成段是大模型味最重的一个特征：把一段推理拆成五个孤立的短句、
  每句自成一段，读起来像幻灯片提纲。
  **正确的做法是让句子之间有连接词、有转折、有推进**，
  让读者跟着一条线走完，而不是跳过五个台阶。
- **加粗要省。** 一段里最多一处加粗，用来标那句真正的结论；
  其余靠句子结构本身承担强调。
- **这两条由 `checks/prose.py` 拦人**（单句段落 ≤35% · 平均每段 ≥1.9 句 ·
  每 ≥110 字一处加粗）。它 2026-08 从报数切成了拦截，因为 26 章全部通过、
  存量清零 —— 而一条零违规的报数规则应该被切成拦截（正文 @sec-rule-worth）。
  **新写完一节先跑 `python3 checks/prose.py` 再提交**，
  它最常抓到的两种回归是"新段落又拆成单句"和"加粗又变密"。
- **表格不能替代叙述。** 表格适合并列的枚举（六个字段、四种失效方式），
  不适合承载因果。**一个「因为 A 所以 B」的论证，写成表格就废了。**
- **图示优先于文字描述结构。** 三段管线、状态机、依赖方向、
  回路的前馈与反馈 —— 这些用 mermaid 一张图胜过两段话。
- **代码示例要真。** 全部来自那个仓库，控制在 15 行以内，
  且必须是能独立读懂的那一段。虚构的示例一律不要。

## 写作纪律

- **每个论断后面挂一个可核验的数字**，没有数字就把论断降级成观察。
  凡是样本量小的地方要说明（「最近 30 个改动」这类）。
- **每个形状至少给一个来自非 Agent 领域的实例**（运维 / 网络 / 数据），
  否则这本书会退化成「iOS + 单体仓库团队专用手册」。
- **标 ⚙️ 的小节需要较重的基建投入**，读者第一遍可以跳过。
  每一部至少有一节是零基建的。
- **作者做错的地方不许删。** 挂了四个月的定时任务、违反自己纪律的那处轮询、
  在磁盘上重复了五次的机制 —— 它们是这本书最有说服力的部分。

## 别做的事

- 别写「本章将介绍」这类过渡段，直接进入内容。
- 别为了对称补一个小节。小节和目录层级一样，必须被内容挣得。
- 别在正文里写「详见第 X 章」的硬编码编号，用 `@sec-` 交叉引用。
- 别用 `sed -i` 批量改稿。跨文件的改动先 grep 出全部命中，逐个打开改。
  这条有过一次实证：一次 `这本书 → 这些` 的批量替换造出了 19 个病句
  （"这些的第一章""而这些宁可你现在不建"），而它们全都能通过 `make check`。
- 别在改写一章之后忘了跑 `checks/voice.py`。新写的段落最容易把
  "这本书""作者"带回来 —— 一次全书改写引入了 43 处。
