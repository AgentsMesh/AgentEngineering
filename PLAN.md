# 完成计划

目标：15 万字，26 章。当前 5,884 字（4%）。

## 排序原则

不按章节号写。按三条排：

1. **代码新鲜度** —— 刚读完哪块源码，就先写吃这块源码的章。
   隔一周再回来，等于重读一遍。
2. **原创度优先** —— 别人抄不走的先写（第 2、17 章已完成骨干），
   因为它们决定这本书值不值得存在，而且最容易写着写着发现自己讲不通。
3. **综合章最后** —— 第 4 章「七个形状」被所有章引用，但它是归纳的产物。
   先写完实例，形状的边界才清楚。前向引用已经在跑，不阻塞。

## 七个波次

### 波次 1 · 立调子 ✅ 已完成

| 章 | 状态 |
|---|---|
| 前言 | 822 / 1,500 |
| 1 两堵墙 | 1,499 / 4,500 |
| 2 不确定性在哪 | 1,871 / 6,000 |
| 17 增益与延迟 | 1,692 / 6,000 |

**这四章不追预算**，它们的作用是把语气、密度、诚实度的标准立住。
等波次 6 回头补齐。

### 波次 2 · 判定层核心（代码证据最硬）

| 章 | 预算 | 必读源码 |
|---|---|---|
| 11 判定的三种失败 | 4,000 | `runner/src/core/outcome.rs` · `orchestrator/render.rs` |
| 13 Guardrails | 10,000 | `architecture/engine/src/service_fallback_contract/` · `config/{raw,validated}` |
| 18 传感器故障 | 6,000 | 各 `*_contract.rs` 里的 `minimum_facts()` |

**退出条件**：`code-map.md` 里五处已挖到的实现，每处都在书里有对应的段落，
且贴出的代码片段都在 15 行以内。

### 波次 3 · 环境层（要新读一批代码）

| 章 | 预算 | 必读源码 |
|---|---|---|
| 7 架构 | 9,000 | API 编译器的 lexer/ir/emit · MicroKernel.swift |
| 6 Codebase | 9,000 | `Modules/*/` 实际形态 · 各产品 BUILD.bazel |
| 8 载体 | 10,000 | skill 投射机制 · `validate_skills.py` |
| 9 工具链 | 9,000 | SynapseKit · Automation · Mainline workflow.Registry |
| 5 环境为什么先于检查 | 4,000 | 无（论证章，最后写） |

**退出条件**：每章至少有一处是从代码里读出来、而原文没有讲的东西。

### 波次 4 · 剩余判定

| 章 | 预算 | 必读源码 |
|---|---|---|
| 12 Tests | 10,000 | Kernel/Tests/*.swift · `ios_ui_test.bzl` |
| 14 ARBITER | 9,000 | `arbiters/src/` · `forbid_pattern/go_projection.rs` |
| 15 规则的生命周期 | 7,000 | 规则的 git 历史（`git log -S` 找每条规则的第一次出现） |

### 波次 5 · 回路收尾

| 章 | 预算 | 必读源码 |
|---|---|---|
| 19 观测器 | 5,000 | Mainline 的 processes 表与注册表 |
| 20 参考输入在环外 | 4,000 | 无 |
| 21 边界 | 4,000 | 无 |

### 波次 6 · 小规模 + 综合 + 补齐

| 章 | 预算 |
|---|---|
| 10 小规模（环境层） | 7,000 |
| 16 小规模（检查层） | 5,000 |
| 3 承诺什么 | 4,000 |
| 4 七个形状 | 7,500 |
| 补齐波次 1 的四章 | +11,000 |

**退出条件**：`make check` 全绿，`make wordcount` 每章都在 ✓。

### 波次 7 · 附录

四份，共 10,000 字。主要是编辑工作 —— 素材直接来自源仓库，
按 `shapes.md` 末尾的脱敏清单处理。

---

## 每章的固定流程

```
1. 读 code-map.md 指定的源码，先读类型再读逻辑，先读测试再读实现
2. 写：正文替换掉 <!-- --> 骨架注释，骨架里的 ⚠️ 是必须保留的论点
3. make check          → 不通过不算写完
4. make wordcount      → 落进预算区间才算写完
5. 一章一 commit，commit message 写这章的 claim
```

**不许跨章编辑。** 跨章的一致性由 `make check` 保证，不由记忆保证。

---

## 判定条件（整本书的验收）

| 条件 | 怎么验 |
|---|---|
| 每章有可证伪的承诺 | `make check-claims` |
| 每章落在预算区间 | `make check-budget` |
| 交叉引用全部有效 | `make check-xref` |
| 术语唯一 | `make check-terms` |
| 中英混排规范 | `make check-typography` |
| 每个形状至少两章出现 | 人工，对照 `shapes.md` 的覆盖矩阵 |
| 每个形状至少一个非 Agent 领域实例 | 人工 |
| 每一部至少一节零基建 | 人工，看 ⚙️ 标记的分布 |
| 作者做错的地方没被删 | 人工。三处：四个月的 panic、那处轮询、磁盘重复五次 |

最后一条是这本书可信度的锚。**任何一次让它变得更好看的修改，都要被拒绝。**
