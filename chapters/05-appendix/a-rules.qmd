---
title: "附录 A · 规则全量表"
claim: "读者能照着这张表，为自己的仓库挑出前五条值得写的规则"
---

# 附录 A · 规则全量表 {#sec-appendix-rules}

<!-- 23 条架构规则。素材直接取自源仓库，按 research/shapes.md 的脱敏清单处理。

     每条给五列：
       - 它守什么
       - 产生它的那次失败（incident 字段，脱敏后）
       - 怎么修（fix_hint 字段）
       - 档位（enforced / report-only）
       - ⚠️ 它换来了什么可测性 —— 这一列是本书加的，源表里没有

     排序不按字母，按「值得先抄哪条」：
       第一梯队（任何团队都该有，零基建）：
         无条件 skip 检测 · 注释掉的代码 · 单一 logger owner · 文件健康度
       第二梯队（有共享层之后）：
         协议优先 · 禁止兜底 fallback · 产品间不互相依赖 · 本地化表归属
       第三梯队（需要构建依赖图）：
         目标粒度 · 构建产物归属 · 生成物来源 · 发布走账本
-->
